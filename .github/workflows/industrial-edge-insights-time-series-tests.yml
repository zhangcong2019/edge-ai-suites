---
# SPDX-FileCopyrightText: (C) 2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

name: "[Industrial Edge Insights Multimodal and Time Series] Functional Tests"
run-name: "[Industrial Edge Insights Multimodal and Time Series] Functional Tests workflow (by @${{ github.actor }} via ${{ github.event_name }})"

on:
  schedule:
    - cron: '0 14 * * *' # 14:00 UTC daily
  workflow_dispatch:
    inputs:
      tag:
        description: 'Branch or tag to checkout (leave empty to use main branch)'
        required: false
        type: string
        default: 'main'
      build:
        description: 'Build Docker images before running tests (yes/no)'
        required: false
        type: string
        default: 'yes'
      weekly_build_date:
        description: 'Weekly build date for Docker images (YYYYMMDD) eg: 20260721. Used only when build=no.'
        required: false
        type: string
        default: ''
      tests:
        description: >
          Which tests to run. Use 'all-tests', 'timeseries-all', 'timeseries-functional', or 'multimodal-all' to run a group,
          or a comma-separated list of file names or pytest node IDs for individual tests.
          File names: test_docker_deployment_stability,
          test_docker_deployment_wind_turbine, test_docker_helm_deployment_security,
          test_docker_influxdb_retention,
          test_helm_deployment_wind_turbine, test_helm_influxdb_retention,
          test_GPU_docker, test_GPU_helm,
          test_docker_deployment_multimodal, test_helm_deployment_multimodal,
          test_GPU_docker_multimodal, test_GPU_helm_multimodal,
          test_NPU_docker_multimodal, test_NPU_helm_multimodal.
          Node ID format (single function): test_docker_deployment_wind_turbine::test_make_up_mqtt.
          Leave empty to use the default set defined in DEFAULT_TESTS env variable.
        required: false
        type: string
      include_gpu_tests:
        description: 'Include GPU tests in all-tests/timeseries-all selectors (yes/no)'
        required: false
        type: string
        default: 'no'
      skip_long_tests:
        description: 'Skip long-running tests that take 1 hour or more (yes/no)'
        required: false
        type: string
        default: 'yes'
      runner_label:
        description: 'Labels for self-hosted or github hosted runners'
        required: false
        type: string
        default: 'ubuntu-24.04'
      suites_repo:
        description: 'Repository to checkout (leave empty to use current repository)'
        required: false
        type: string
      suites_ref:
        description: 'Branch or tag to checkout from suites_repo (leave empty to use tag input)'
        required: false
        type: string
  workflow_call:
    inputs:
      tag:
        description: 'Branch or tag to checkout (leave empty to use main branch)'
        required: false
        type: string
        default: 'main'
      build:
        description: 'Build Docker images before running tests (yes/no)'
        required: false
        type: string
        default: 'yes'
      weekly_build_date:
        description: 'Weekly build date for Docker images (YYYYMMDD) eg: 20260721. Used only when build=no.'
        required: false
        type: string
        default: ''
      tests:
        description: >
          Which tests to run. Use 'all-tests', 'timeseries-all', 'timeseries-functional', or 'multimodal-all' to run a group,
          or a comma-separated list of file names or pytest node IDs for individual tests.
          File names: test_docker_deployment_stability,
          test_docker_deployment_wind_turbine, test_docker_helm_deployment_security,
          test_docker_influxdb_retention,
          test_helm_deployment_wind_turbine, test_helm_influxdb_retention,
          test_GPU_docker, test_GPU_helm,
          test_docker_deployment_multimodal, test_helm_deployment_multimodal,
          test_GPU_docker_multimodal, test_GPU_helm_multimodal,
          test_NPU_docker_multimodal, test_NPU_helm_multimodal.
          Node ID format (single function): test_docker_deployment_wind_turbine::test_make_up_mqtt.
          Leave empty to use the default set defined in DEFAULT_TESTS env variable.
        required: false
        type: string
      include_gpu_tests:
        description: 'Include GPU tests in all-tests/timeseries-all selectors (yes/no)'
        required: false
        type: string
        default: 'no'
      skip_long_tests:
        description: 'Skip long-running tests that take 1 hour or more (yes/no)'
        required: false
        type: string
        default: 'yes'
      runner_label:
        description: 'Labels for self-hosted or github hosted runners'
        required: false
        type: string
        default: 'ubuntu-24.04'
      suites_repo:
        description: 'Repository to checkout (leave empty to use current repository)'
        required: false
        type: string
      suites_ref:
        description: 'Branch or tag to checkout from suites_repo (leave empty to use tag input)'
        required: false
        type: string
permissions: {}

env:
  DEFAULT_TESTS: all-tests

jobs:
  functional-tests:
    name: Functional Tests for Multimodal and TimeSeries
    runs-on: ${{ inputs.runner_label || 'ubuntu-24.04' }}
    env:
      KUBECONFIG: /etc/rancher/k3s/k3s.yaml
      WEEKLY_BUILD_DATE_INPUT: ${{ inputs.weekly_build_date || '' }}
    permissions:
      contents: read
      packages: read
    steps:
      - name: Print workflow inputs
        env:
          TAG: ${{ inputs.tag || github.ref }}
          BUILD: ${{ inputs.build || 'yes' }}
          TESTS: ${{ inputs.tests || env.DEFAULT_TESTS }}
          INCLUDE_GPU_TESTS: ${{ inputs.include_gpu_tests || 'no' }}
          SKIP_LONG_TESTS: ${{ inputs.skip_long_tests || 'yes' }}
        run: |
          echo "tag/ref         : $TAG"
          echo "build           : $BUILD"
          echo "tests           : $TESTS"
          echo "include_gpu_tests: $INCLUDE_GPU_TESTS"
          echo "skip_long_tests : $SKIP_LONG_TESTS"
          # Normalize build value (trim whitespace) so all downstream if: conditions work correctly
          BUILD_NORMALIZED="$(echo "$BUILD" | xargs)"
          echo "BUILD_NORMALIZED=${BUILD_NORMALIZED}" >> "$GITHUB_ENV"

      - name: Validate weekly_build_date input usage
        run: |
          # BUILD_NORMALIZED is set by the previous step (whitespace-trimmed)
          if [[ -n "$WEEKLY_BUILD_DATE_INPUT" && "$BUILD_NORMALIZED" != "no" ]]; then
            echo "Error: 'weekly_build_date' is only supported when build=no."
            echo "Current values: build=$BUILD_NORMALIZED, weekly_build_date=$WEEKLY_BUILD_DATE_INPUT"
            exit 1
          fi

      - name: Checkout edge-ai-suites repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: ${{ inputs.suites_repo || github.repository }}
          ref: ${{ inputs.suites_ref || inputs.tag || github.ref }}
          persist-credentials: false

      - name: Set weekly build date in .env files
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'no' && env.WEEKLY_BUILD_DATE_INPUT != '' }}
        run: |
          # docker-compose reads WEEKLY_BUILD_DATE from .env to build the image tag
          # (${IMAGE_SUFFIX}-${WEEKLY_BUILD_DATE}-weekly). Without this, docker-compose
          # falls back to the undated ${IMAGE_SUFFIX} tag instead of the requested
          # weekly build, so it must be kept in sync with the workflow input.
          for env_file in \
            "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/.env" \
            "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-multimodal/.env"; do
            if [[ -f "$env_file" ]]; then
              sed -i "s/^WEEKLY_BUILD_DATE=.*/WEEKLY_BUILD_DATE=${WEEKLY_BUILD_DATE_INPUT}/" "$env_file"
              echo "Updated $env_file -> WEEKLY_BUILD_DATE=${WEEKLY_BUILD_DATE_INPUT}"
            else
              echo "Warning: $env_file not found, skipping"
            fi
          done

      - name: Resolve edge-ai-libraries ref
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'yes' }}
        env:
          REQUESTED_TAG: ${{ inputs.tag }}
        run: |
          LIBRARIES_REF="main"
          if [[ -n "$REQUESTED_TAG" ]]; then
            if git ls-remote --exit-code --heads --tags https://github.com/open-edge-platform/edge-ai-libraries.git "$REQUESTED_TAG" >/dev/null 2>&1; then
              LIBRARIES_REF="$REQUESTED_TAG"
            else
              echo "Requested edge-ai-libraries ref '$REQUESTED_TAG' was not found. Falling back to 'main'."
            fi
          fi
          echo "EDGE_AI_LIBRARIES_REF=$LIBRARIES_REF" >> "$GITHUB_ENV"

      - name: Checkout Time Series Analytics microservice (edge-ai-libraries)
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'yes' }}
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: open-edge-platform/edge-ai-libraries
          ref: ${{ env.EDGE_AI_LIBRARIES_REF }}
          path: edge-ai-libraries
          persist-credentials: false

      - name: Building Time Series Analytics microservices
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'yes' }}
        run: |
          cd ./edge-ai-libraries/microservices/time-series-analytics/docker
          docker compose down -v
          docker compose build

      - name: Building Multimodal Sample App images
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'yes' }}
        run: |
          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-multimodal/"
          make down
          make build

      - name: Generate Helm charts for Multimodal Sample App
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'yes' }}
        run: |
          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-multimodal/"
          echo "Generating Helm charts for multimodal..."
          make gen_helm_charts

      - name: Building Time Series Sample App images
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'yes' }}
        run: |
          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/"
          make down
          make build

      - name: Generate Helm charts for Time Series Sample Apps
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'yes' }}
        env:
          TESTS: ${{ inputs.tests || env.DEFAULT_TESTS }}
        run: |
          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/"

          if echo "$TESTS" | grep -qwE "all-tests|timeseries-all|timeseries-functional|test_helm_deployment_wind_turbine|test_helm_influxdb_retention|test_docker_helm_deployment_security|test_GPU_helm"; then
            echo "Generating Helm chart package for wind-turbine-anomaly-detection..."
            make gen_helm_charts_targz app=wind-turbine-anomaly-detection
            TS_TGZ=$(ls -t helm-packages/*.tgz | head -1)
            echo "Extracting $TS_TGZ into helm-packages/ for direct use by tests..."
            tar -xzf "$TS_TGZ" -C helm-packages/ --strip-components=1
          else
            echo "No helm tests selected, skipping helm chart generation"
          fi

      - name: Pull Helm charts from OCI registry
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'no' }}
        env:
          TESTS: ${{ inputs.tests || env.DEFAULT_TESTS }}
        run: |
          # Pull the chart .tgz straight into the helm-packages directory used by tests,
          # then extract it in place so tests can read/edit values.yaml and install
          # directly from the directory without any extraction logic of their own.
          pull_chart() {
            local oci_ref="$1"
            local chart_version="$2"
            local dest_dir="$3"
            mkdir -p "$dest_dir"
            echo "Pulling $oci_ref:$chart_version ..."
            helm pull "$oci_ref" --version "$chart_version" --destination "$dest_dir"
            local pulled_tgz
            pulled_tgz=$(ls -t "$dest_dir"/*.tgz | head -1)
            echo "Pulled chart: $pulled_tgz"
            echo "Extracting $pulled_tgz into $dest_dir for direct use by tests..."
            tar -xzf "$pulled_tgz" -C "$dest_dir" --strip-components=1
          }

          if echo "$TESTS" | grep -qwE "all-tests|timeseries-all|timeseries-functional|test_helm_deployment_wind_turbine|test_helm_influxdb_retention|test_docker_helm_deployment_security|test_GPU_helm"; then
            TS_IMAGE_SUFFIX=$(grep 'image_suffix:' \
              "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/helm/values.yaml" \
              | head -1 | awk -F'"' '{print $2}')
            TS_CHART_VERSION="${TS_IMAGE_SUFFIX}-${WEEKLY_BUILD_DATE_INPUT}-weekly"
            echo "Time Series Helm chart pull: image_suffix=$TS_IMAGE_SUFFIX chart_version=$TS_CHART_VERSION"
            pull_chart \
              "oci://registry-1.docker.io/intel/wind-turbine-anomaly-detection-sample-app" \
              "$TS_CHART_VERSION" \
              "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/helm-packages"
          else
            echo "No Time Series Helm tests selected, skipping Time Series chart pull"
          fi

          if echo "$TESTS" | grep -qwE "all-tests|multimodal-all|test_helm_deployment_multimodal|test_GPU_helm_multimodal|test_NPU_helm_multimodal"; then
            MM_IMAGE_SUFFIX=$(grep 'image_suffix:' \
              "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-multimodal/helm/values.yaml" \
              | head -1 | awk -F'"' '{print $2}')
            MM_CHART_VERSION="${MM_IMAGE_SUFFIX}-${WEEKLY_BUILD_DATE_INPUT}-weekly"
            echo "Multimodal Helm chart pull: image_suffix=$MM_IMAGE_SUFFIX chart_version=$MM_CHART_VERSION"
            pull_chart \
              "oci://registry-1.docker.io/intel/multimodal-weld-defect-detection-sample-app" \
              "$MM_CHART_VERSION" \
              "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-multimodal/helm-packages"
          else
            echo "No Multimodal Helm tests selected, skipping Multimodal chart pull"
          fi

      - name: Install nmap
        run: |
          sudo apt-get update
          sudo apt-get install -y nmap

      - name: Install k3s
        run: |
          curl -sfL https://get.k3s.io | INSTALL_K3S_SELINUX_WARN=true INSTALL_K3S_VERSION=${K3S_VERSION} \
          sh -s - --disable=traefik --write-kubeconfig-mode=644
          test -r "$KUBECONFIG"

      - name: Load images into k3s
        if: ${{ (env.BUILD_NORMALIZED || 'yes') == 'yes' }}
        run: |
          echo "Loading docker images into k3s..."
          docker images --format "{{.Repository}}:{{.Tag}}" | grep "^intel/ia-" | grep -v "<none>" | while read image; do
              echo "Importing image: $image"
              docker save "$image" | sudo k3s ctr images import -
          done

      - name: Cleanup cluster before Time Series Wind and Weld tests
        run: |
          echo "=== Cleaning up cluster resources before Time Series Wind and Weld tests ==="

          # Uninstall all helm releases
          echo "Uninstalling all Helm releases..."
          helm list --all-namespaces -q 2>/dev/null | while read release; do
            ns=$(helm list --all-namespaces --filter "^${release}$" -o json 2>/dev/null | jq -r '.[0].namespace // "default"')
            echo "Uninstalling helm release: $release from namespace: $ns"
            helm uninstall "$release" -n "$ns" --wait --timeout 5m 2>/dev/null || true
          done

          # Delete test-related namespaces (ts-sample-app is used by security tests)
          echo "Cleaning up test namespaces..."
          for ns in ts-sample-app weld wind timeseries; do
            if kubectl get namespace "$ns" 2>/dev/null; then
              echo "Deleting namespace: $ns"
              kubectl delete namespace "$ns" --wait=true --timeout=5m 2>/dev/null || true
            fi
          done

          # Clean up sample-app namespaces
          echo "Cleaning up namespace-scoped test resources..."
          for ns in ts-sample-app weld wind timeseries; do
            if kubectl get namespace "$ns" 2>/dev/null; then
              kubectl delete pvc --all -n "$ns" --wait=true --timeout=5m 2>/dev/null || true
              kubectl wait --for=delete pod --all -n "$ns" --timeout=5m 2>/dev/null || true
              kubectl get pods -n "$ns" -o name 2>/dev/null | while read pod; do
                kubectl delete "$pod" -n "$ns" --force --grace-period=0 2>/dev/null || true
              done
            fi
          done

          # Clean up any leftover docker resources
          echo "Cleaning up docker resources..."
          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/"
          make down 2>/dev/null || true

          # Give cluster time to stabilize
          echo "Waiting for cluster to stabilize..."
          sleep 30

          # Verify cluster is healthy
          echo "Verifying cluster health..."
          test -r "$KUBECONFIG"
          kubectl get nodes
          kubectl get pods -A
          echo "=== Time Series Wind and Weld cleanup complete ==="

      - name: Run Time Series Wind and Weld functional tests
        env:
          TESTS: ${{ inputs.tests || env.DEFAULT_TESTS }}
          INCLUDE_GPU_TESTS: ${{ inputs.include_gpu_tests || 'no' }}
          SKIP_LONG_TESTS: ${{ inputs.skip_long_tests || 'yes' }}
        run: |
          # Core time-series test files excluding security.
          TIMESERIES_CORE_TESTS="test_docker_deployment_stability \
            test_docker_deployment_wind_turbine test_docker_influxdb_retention \
            test_helm_deployment_wind_turbine test_GPU_docker test_GPU_helm \
            test_helm_influxdb_retention"

          # Skip if only multimodal tests are selected
          # Parse comma-separated input, trim whitespace from each entry
          IFS=',' read -ra SELECTED <<< "$TESTS"
          SELECTED_TESTS=()
          for t in "${SELECTED[@]}"; do
            t="$(echo "$t" | xargs)"
            [[ -n "$t" ]] && SELECTED_TESTS+=("$t")
          done

          # Determine which core time-series test files to run.
          # Supports file names (test_foo), file names with .py, and node IDs (test_foo::test_bar)
          TS_CORE_TEST_FILES=()
          RUN_ALL_TS=false
          for t in "${SELECTED_TESTS[@]}"; do
            if [[ "$t" == "all-tests" || "$t" == "timeseries-all" || "$t" == "timeseries-functional" ]]; then
              RUN_ALL_TS=true; break
            fi
            # Extract base file name (strip .py suffix and anything after ::)
            file_part="${t%%::*}"
            file_part="${file_part%.py}"
            if echo "$TIMESERIES_CORE_TESTS" | grep -qw "$file_part"; then
              if [[ "$t" == *"::"* ]]; then
                TS_CORE_TEST_FILES+=("${file_part}.py::${t#*::}")
              else
                TS_CORE_TEST_FILES+=("${file_part}.py")
              fi
            fi
          done

          if [[ "$RUN_ALL_TS" == "false" && "${#TS_CORE_TEST_FILES[@]}" -eq 0 ]]; then
            echo "Skipping time-series core tests (no wind/weld tests selected)"
            exit 0
          fi

          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/tests/functional"
          echo "Running Time Series Wind and Weld functional tests"
          python3 -m venv env
          source env/bin/activate
          pip3 install -r ../requirements.txt
          mkdir -p /tmp/test_timeseries_core_report
          rm -f /tmp/test_timeseries_core_report/report.html /tmp/test_timeseries_core_report/junit.xml

          # Build marker expression to skip long-running tests when requested
          MARKER_ARGS=()
          if [[ "$SKIP_LONG_TESTS" == "yes" ]]; then
            echo "Skipping long-running tests (marked with @pytest.mark.longrun)"
            MARKER_ARGS=(-m "not longrun")
          fi

          # When running the full suite, ignore GPU tests unless explicitly enabled.
          # GPU test files remain individually selectable via the tests input.
          IGNORE_ARGS=()
          if [[ "$RUN_ALL_TS" == "true" && "$INCLUDE_GPU_TESTS" != "yes" ]]; then
            IGNORE_ARGS=(
              --ignore=test_GPU_docker.py
              --ignore=test_GPU_helm.py
            )
          fi

          # For weekly builds, skip KPI/chart-generation tests that are not applicable.
          WEEKLY_EXCLUDE_ARGS=()
          if [[ -n "$WEEKLY_BUILD_DATE_INPUT" ]]; then
            echo "weekly_build_date is set (${WEEKLY_BUILD_DATE_INPUT}); excluding image-build and chart-generation tests (not applicable for weekly builds)"
            WEEKLY_EXCLUDE_ARGS=(
              -k "not test_container_sizes_kpi and not test_build_time_kpi and not test_gen_chart"
            )
          fi

          # List tests that will be collected (helps debug which tests run/are skipped)
          echo ""
          echo "========== Collecting time-series core tests =========="
          if [[ "$RUN_ALL_TS" == "true" ]]; then
            pytest --collect-only -q "${MARKER_ARGS[@]}" "${WEEKLY_EXCLUDE_ARGS[@]}" "${IGNORE_ARGS[@]}" 2>&1 || true
          else
            pytest --collect-only -q "${MARKER_ARGS[@]}" "${WEEKLY_EXCLUDE_ARGS[@]}" "${TS_CORE_TEST_FILES[@]}" 2>&1 || true
          fi
          echo "======================================"
          echo ""

          # Run selected tests (all or specific files)
          set +e
          if [[ "$RUN_ALL_TS" == "true" ]]; then
            pytest -vv --tb=short --self-contained-html --html=/tmp/test_timeseries_core_report/report.html \
              --junitxml=/tmp/test_timeseries_core_report/junit.xml "${MARKER_ARGS[@]}" "${WEEKLY_EXCLUDE_ARGS[@]}" "${IGNORE_ARGS[@]}" \
              --ignore=test_docker_helm_deployment_security.py
          else
            pytest -vv --tb=short --self-contained-html --html=/tmp/test_timeseries_core_report/report.html \
              --junitxml=/tmp/test_timeseries_core_report/junit.xml "${MARKER_ARGS[@]}" "${WEEKLY_EXCLUDE_ARGS[@]}" "${TS_CORE_TEST_FILES[@]}"
          fi
          PYTEST_RC=$?
          set -e
          echo "TIMESERIES_CORE_EXIT_CODE=$PYTEST_RC" >> "$GITHUB_ENV"

          if [[ "$PYTEST_RC" -ne 0 ]]; then
            echo ""
            echo "========== Time Series Core Failure Diagnostics =========="
            echo "Kubernetes nodes:"
            kubectl get nodes -o wide 2>&1 || true
            echo ""
            echo "All pods:"
            kubectl get pods -A -o wide 2>&1 || true
            echo ""
            echo "Time-series namespace resources (ts-sample-app):"
            kubectl get all -n ts-sample-app -o wide 2>&1 || true
            echo ""
            echo "Time-series namespace pod descriptions:"
            kubectl describe pods -n ts-sample-app 2>&1 || true
            echo ""
            echo "Time-series namespace recent events:"
            kubectl get events -n ts-sample-app --sort-by=.lastTimestamp 2>&1 || true
            echo ""
            echo "Time-series namespace current pod logs:"
            for deploy in \
              deployment-time-series-analytics-microservice \
              deployment-telegraf \
              deployment-influxdb \
              deployment-nginx \
              deployment-mqtt-publisher \
              deployment-mqtt-broker \
              deployment-opcua-server \
              deployment-grafana; do
              echo "----- Logs for ${deploy} -----"
              kubectl logs -n ts-sample-app --all-containers=true --prefix=true --tail=200 "deployment/${deploy}" 2>&1 || true
            done
            echo ""
            echo "Pytest-generated tests.log (if present):"
            find . -maxdepth 3 -name tests.log -type f -print -exec tail -n 400 {} \; 2>&1 || true
            echo "===================================================="
            echo ""
          fi

          # Print test results summary with failure details
          python3 - <<'PYEOF'
          import xml.etree.ElementTree as ET, os
          from collections import defaultdict
          def fmt_time(s):
              s = float(s); m = int(s) // 60; r = s - m * 60
              return f"{m}m{r:.0f}s" if m else f"{s:.1f}s"
          xml_path = '/tmp/test_timeseries_core_report/junit.xml'
          if not os.path.exists(xml_path):
              print("No test results found (JUnit XML not generated)")
          else:
              root = ET.parse(xml_path).getroot()
              files = defaultdict(lambda: {'total': 0, 'passed': 0, 'failed': 0, 'skipped': 0, 'errors': 0, 'time': 0.0})
              failures = []
              errors = []
              for tc in root.findall('.//testcase'):
                  parts = tc.get('classname', '').split('.')
                  fname = next((p for p in parts if p.startswith('test_')), None)
                  if not fname:
                      raw_name = tc.get('name', parts[0] if parts else 'unknown')
                      fname = raw_name.replace('.py', '') if raw_name else 'unknown'
                  test_id = f"{fname}::{tc.get('name', 'unknown')}"
                  files[fname]['total'] += 1
                  files[fname]['time'] += float(tc.get('time', 0))
                  fail_el = tc.find('failure')
                  err_el = tc.find('error')
                  if fail_el is not None:
                      files[fname]['failed'] += 1
                      msg = fail_el.get('message', '')[:200]
                      failures.append(f"  FAILED  {test_id}\n          {msg}")
                  elif err_el is not None:
                      files[fname]['errors'] += 1
                      msg = err_el.get('message', '')[:200]
                      errors.append(f"  ERROR   {test_id}\n          {msg}")
                  elif tc.find('skipped') is not None:
                      files[fname]['skipped'] += 1
                  else:
                      files[fname]['passed'] += 1
              print(f"\n{'Test File':<55} {'Run':>5} {'Pass':>5} {'Fail':>5} {'Skip':>5} {'Err':>5} {'Time':>8}")
              print('-' * 90)
              for fname, c in sorted(files.items()):
                  print(f"{fname:<55} {c['total']:>5} {c['passed']:>5} {c['failed']:>5} {c['skipped']:>5} {c['errors']:>5} {fmt_time(c['time']):>8}")
              if failures:
                  print(f"\n--- Failures ({len(failures)}) ---")
                  print('\n'.join(failures))
              if errors:
                  print(f"\n--- Errors ({len(errors)}) ---")
                  print('\n'.join(errors))
              print()
          PYEOF
          deactivate
          exit $PYTEST_RC

      - name: Upload Time Series Wind and Weld HTML test report to GitHub
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882 # v4.4.3
        with:
          name: timeseries-core-test-report
          path: /tmp/test_timeseries_core_report

      - name: Cleanup cluster before security tests
        if: ${{ !cancelled() }}
        run: |
          echo "=== Cleaning up cluster resources before security tests ==="

          echo "Uninstalling all Helm releases..."
          helm list --all-namespaces -q 2>/dev/null | while read release; do
            ns=$(helm list --all-namespaces --filter "^${release}$" -o json 2>/dev/null | jq -r '.[0].namespace // "default"')
            echo "Uninstalling helm release: $release from namespace: $ns"
            helm uninstall "$release" -n "$ns" --wait --timeout 5m 2>/dev/null || true
          done

          echo "Cleaning up security test namespace..."
          if kubectl get namespace ts-sample-app 2>/dev/null; then
            kubectl delete namespace ts-sample-app --wait=true --timeout=5m 2>/dev/null || true
          fi

          echo "Cleaning up namespace-scoped test resources..."
          if kubectl get namespace ts-sample-app 2>/dev/null; then
            kubectl delete pvc --all -n ts-sample-app --wait=true --timeout=5m 2>/dev/null || true
            kubectl wait --for=delete pod --all -n ts-sample-app --timeout=5m 2>/dev/null || true
            kubectl get pods -n ts-sample-app -o name 2>/dev/null | while read pod; do
              kubectl delete "$pod" -n ts-sample-app --force --grace-period=0 2>/dev/null || true
            done
          fi

          echo "Cleaning up docker resources..."
          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/"
          make down 2>/dev/null || true

          echo "Waiting for cluster to stabilize..."
          sleep 30

          echo "Verifying cluster health..."
          test -r "$KUBECONFIG"
          kubectl get nodes
          kubectl get pods -A
          echo "=== Security cleanup complete ==="

      - name: Run Security functional tests
        if: ${{ !cancelled() }}
        env:
          TESTS: ${{ inputs.tests || env.DEFAULT_TESTS }}
          SKIP_LONG_TESTS: ${{ inputs.skip_long_tests || 'yes' }}
        run: |
          SECURITY_TEST_FILE="test_docker_helm_deployment_security"

          IFS=',' read -ra SELECTED <<< "$TESTS"
          SECURITY_TEST_FILES=()
          RUN_SECURITY=false
          for t in "${SELECTED[@]}"; do
            t="$(echo "$t" | xargs)"
            [[ -z "$t" ]] && continue
            if [[ "$t" == "all-tests" || "$t" == "timeseries-all" ]]; then
              RUN_SECURITY=true
              break
            fi
            file_part="${t%%::*}"
            file_part="${file_part%.py}"
            if [[ "$file_part" == "$SECURITY_TEST_FILE" ]]; then
              if [[ "$t" == *"::"* ]]; then
                SECURITY_TEST_FILES+=("${file_part}.py::${t#*::}")
              else
                SECURITY_TEST_FILES+=("${file_part}.py")
              fi
            fi
          done

          if [[ "$RUN_SECURITY" == "false" && "${#SECURITY_TEST_FILES[@]}" -eq 0 ]]; then
            echo "Skipping security tests (no security tests selected)"
            exit 0
          fi

          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/tests/functional"
          echo "Running Security functional tests"
          python3 -m venv env
          source env/bin/activate
          pip3 install -r ../requirements.txt
          mkdir -p /tmp/test_timeseries_security_report
          rm -f /tmp/test_timeseries_security_report/report.html /tmp/test_timeseries_security_report/junit.xml

          MARKER_ARGS=()
          if [[ "$SKIP_LONG_TESTS" == "yes" ]]; then
            echo "Skipping long-running tests (marked with @pytest.mark.longrun)"
            MARKER_ARGS=(-m "not longrun")
          fi

          echo ""
          echo "========== Collecting security tests =========="
          if [[ "$RUN_SECURITY" == "true" ]]; then
            pytest --collect-only -q "${MARKER_ARGS[@]}" test_docker_helm_deployment_security.py 2>&1 || true
          else
            pytest --collect-only -q "${MARKER_ARGS[@]}" "${SECURITY_TEST_FILES[@]}" 2>&1 || true
          fi
          echo "==========================================="
          echo ""

          set +e
          if [[ "$RUN_SECURITY" == "true" ]]; then
            pytest -vv --tb=short --self-contained-html --html=/tmp/test_timeseries_security_report/report.html \
              --junitxml=/tmp/test_timeseries_security_report/junit.xml "${MARKER_ARGS[@]}" test_docker_helm_deployment_security.py
          else
            pytest -vv --tb=short --self-contained-html --html=/tmp/test_timeseries_security_report/report.html \
              --junitxml=/tmp/test_timeseries_security_report/junit.xml "${MARKER_ARGS[@]}" "${SECURITY_TEST_FILES[@]}"
          fi
          PYTEST_RC=$?
          set -e
          echo "TIMESERIES_SECURITY_EXIT_CODE=$PYTEST_RC" >> "$GITHUB_ENV"

          if [[ "$PYTEST_RC" -ne 0 ]]; then
            echo ""
            echo "========== Security Failure Diagnostics =========="
            echo "Kubernetes nodes:"
            kubectl get nodes -o wide 2>&1 || true
            echo ""
            echo "All pods:"
            kubectl get pods -A -o wide 2>&1 || true
            echo ""
            echo "Time-series namespace resources (ts-sample-app):"
            kubectl get all -n ts-sample-app -o wide 2>&1 || true
            echo ""
            echo "Time-series namespace pod descriptions:"
            kubectl describe pods -n ts-sample-app 2>&1 || true
            echo ""
            echo "Time-series namespace recent events:"
            kubectl get events -n ts-sample-app --sort-by=.lastTimestamp 2>&1 || true
            echo ""
            echo "Time-series namespace current pod logs:"
            for deploy in \
              deployment-time-series-analytics-microservice \
              deployment-telegraf \
              deployment-influxdb \
              deployment-nginx \
              deployment-mqtt-publisher \
              deployment-mqtt-broker \
              deployment-opcua-server \
              deployment-grafana; do
              echo "----- Logs for ${deploy} -----"
              kubectl logs -n ts-sample-app --all-containers=true --prefix=true --tail=200 "deployment/${deploy}" 2>&1 || true
            done
            echo ""
            echo "Pytest-generated tests.log (if present):"
            find . -maxdepth 3 -name tests.log -type f -print -exec tail -n 400 {} \; 2>&1 || true
            echo "========================================================"
            echo ""
          fi

          python3 - <<'PYEOF'
          import xml.etree.ElementTree as ET, os
          from collections import defaultdict
          def fmt_time(s):
              s = float(s); m = int(s) // 60; r = s - m * 60
              return f"{m}m{r:.0f}s" if m else f"{s:.1f}s"
          xml_path = '/tmp/test_timeseries_security_report/junit.xml'
          if not os.path.exists(xml_path):
              print("No test results found (JUnit XML not generated)")
          else:
              root = ET.parse(xml_path).getroot()
              files = defaultdict(lambda: {'total': 0, 'passed': 0, 'failed': 0, 'skipped': 0, 'errors': 0, 'time': 0.0})
              failures = []
              errors = []
              for tc in root.findall('.//testcase'):
                  parts = tc.get('classname', '').split('.')
                  fname = next((p for p in parts if p.startswith('test_')), None)
                  if not fname:
                      raw_name = tc.get('name', parts[0] if parts else 'unknown')
                      fname = raw_name.replace('.py', '') if raw_name else 'unknown'
                  test_id = f"{fname}::{tc.get('name', 'unknown')}"
                  files[fname]['total'] += 1
                  files[fname]['time'] += float(tc.get('time', 0))
                  fail_el = tc.find('failure')
                  err_el = tc.find('error')
                  if fail_el is not None:
                      files[fname]['failed'] += 1
                      msg = fail_el.get('message', '')[:200]
                      failures.append(f"  FAILED  {test_id}\n          {msg}")
                  elif err_el is not None:
                      files[fname]['errors'] += 1
                      msg = err_el.get('message', '')[:200]
                      errors.append(f"  ERROR   {test_id}\n          {msg}")
                  elif tc.find('skipped') is not None:
                      files[fname]['skipped'] += 1
                  else:
                      files[fname]['passed'] += 1
              print(f"\n{'Test File':<55} {'Run':>5} {'Pass':>5} {'Fail':>5} {'Skip':>5} {'Err':>5} {'Time':>8}")
              print('-' * 90)
              for fname, c in sorted(files.items()):
                  print(f"{fname:<55} {c['total']:>5} {c['passed']:>5} {c['failed']:>5} {c['skipped']:>5} {c['errors']:>5} {fmt_time(c['time']):>8}")
              if failures:
                  print(f"\n--- Failures ({len(failures)}) ---")
                  print('\n'.join(failures))
              if errors:
                  print(f"\n--- Errors ({len(errors)}) ---")
                  print('\n'.join(errors))
              print()
          PYEOF
          deactivate
          exit $PYTEST_RC

      - name: Upload Security HTML test report to GitHub
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882 # v4.4.3
        with:
          name: security-test-report
          path: /tmp/test_timeseries_security_report

      - name: Cleanup cluster before multimodal tests
        if: ${{ !cancelled() }}
        run: |
          echo "=== Cleaning up cluster resources before multimodal tests ==="

          # Uninstall all helm releases in default namespace
          echo "Uninstalling all Helm releases..."
          helm list --all-namespaces -q 2>/dev/null | while read release; do
            ns=$(helm list --all-namespaces --filter "^${release}$" -o json 2>/dev/null | jq -r '.[0].namespace // "default"')
            echo "Uninstalling helm release: $release from namespace: $ns"
            helm uninstall "$release" -n "$ns" --wait --timeout 5m 2>/dev/null || true
          done

          # Delete test-related namespaces (if any were created)
          echo "Cleaning up test namespaces..."
          kubectl get namespaces -o name 2>/dev/null | grep -E "(weld|wind|timeseries|multimodal)" | while read ns; do
            echo "Deleting namespace: $ns"
            kubectl delete "$ns" --wait=true --timeout=5m 2>/dev/null || true
          done

          # Clean up sample-app namespaces
          echo "Cleaning up namespace-scoped test resources..."
          for ns in ts-sample-app weld wind timeseries multimodal; do
            if kubectl get namespace "$ns" 2>/dev/null; then
              kubectl delete pvc --all -n "$ns" --wait=true --timeout=5m 2>/dev/null || true
              kubectl wait --for=delete pod --all -n "$ns" --timeout=5m 2>/dev/null || true
              kubectl get pods -n "$ns" -o name 2>/dev/null | while read pod; do
                kubectl delete "$pod" -n "$ns" --force --grace-period=0 2>/dev/null || true
              done
            fi
          done

          # Clean up any leftover docker resources from prior segments
          echo "Cleaning up docker resources..."
          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-time-series/"
          make down 2>/dev/null || true
          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-multimodal/"
          make down 2>/dev/null || true

          # Give cluster time to stabilize
          echo "Waiting for cluster to stabilize..."
          sleep 30

          # Verify cluster is healthy
          echo "Verifying cluster health..."
          test -r "$KUBECONFIG"
          kubectl get nodes
          kubectl get pods -A
          echo "=== Multimodal cleanup complete ==="

      - name: Run Multimodal Weld Defect Detection Sample Apps functional tests
        if: ${{ !cancelled() }}
        env:
          TESTS: ${{ inputs.tests || env.DEFAULT_TESTS }}
          SKIP_LONG_TESTS: ${{ inputs.skip_long_tests || 'yes' }}
        run: |
          # Multimodal-specific test files
          MULTIMODAL_TESTS="test_docker_deployment_multimodal test_helm_deployment_multimodal \
            test_GPU_docker_multimodal test_GPU_helm_multimodal \
            test_NPU_docker_multimodal test_NPU_helm_multimodal"

          # Skip if only timeseries tests are selected
          # Parse comma-separated input, trim whitespace from each entry
          IFS=',' read -ra SELECTED <<< "$TESTS"
          SELECTED_TESTS=()
          for t in "${SELECTED[@]}"; do
            t="$(echo "$t" | xargs)"
            [[ -n "$t" ]] && SELECTED_TESTS+=("$t")
          done

          # Determine which multimodal test files to run
          # Supports file names (test_foo), file names with .py, and node IDs (test_foo::test_bar)
          MM_TEST_FILES=()
          RUN_ALL_MM=false
          for t in "${SELECTED_TESTS[@]}"; do
            if [[ "$t" == "all-tests" || "$t" == "multimodal-all" ]]; then
              RUN_ALL_MM=true; break
            fi
            # Extract base file name (strip .py suffix and anything after ::)
            file_part="${t%%::*}"
            file_part="${file_part%.py}"
            if echo "$MULTIMODAL_TESTS" | grep -qw "$file_part"; then
              if [[ "$t" == *"::"* ]]; then
                MM_TEST_FILES+=("${file_part}.py::${t#*::}")
              else
                MM_TEST_FILES+=("${file_part}.py")
              fi
            fi
          done

          if [[ "$RUN_ALL_MM" == "false" && "${#MM_TEST_FILES[@]}" -eq 0 ]]; then
            echo "Skipping multimodal tests (no multimodal tests selected)"
            exit 0
          fi

          cd "${{ github.workspace }}/manufacturing-ai-suite/industrial-edge-insights-multimodal/tests/functional"
          echo "Running Multimodal Weld Defect Detection Sample Apps functional tests"
          python3 -m venv env
          source env/bin/activate
          pip3 install -r ../requirements.txt
          mkdir -p /tmp/test_multimodal_report
          rm -f /tmp/test_multimodal_report/report.html /tmp/test_multimodal_report/junit.xml

          # Build marker expression to skip long-running tests when requested
          MARKER_ARGS=()
          if [[ "$SKIP_LONG_TESTS" == "yes" ]]; then
            echo "Skipping long-running tests (marked with @pytest.mark.longrun)"
            MARKER_ARGS=(-m "not longrun")
          fi

          # When running the full suite (all-tests / multimodal-all), exclude GPU and NPU
          # test files unless explicitly opted in. They remain individually selectable
          # via the tests input (e.g. tests=test_GPU_helm_multimodal).
          IGNORE_ARGS=()
          if [[ "$RUN_ALL_MM" == "true" && "$INCLUDE_GPU_TESTS" != "yes" ]]; then
            IGNORE_ARGS=(
              --ignore=test_GPU_docker_multimodal.py
              --ignore=test_GPU_helm_multimodal.py
              --ignore=test_NPU_docker_multimodal.py
              --ignore=test_NPU_helm_multimodal.py
            )
          fi

          # List tests that will be collected (helps debug which tests run/are skipped)
          echo ""
          echo "========== Collecting tests =========="
          if [[ "$RUN_ALL_MM" == "true" ]]; then
            pytest --collect-only -q "${MARKER_ARGS[@]}" "${IGNORE_ARGS[@]}" 2>&1 || true
          else
            pytest --collect-only -q "${MARKER_ARGS[@]}" "${MM_TEST_FILES[@]}" 2>&1 || true
          fi
          echo "======================================"
          echo ""

          # Run selected tests (all or specific files)
          set +e
          if [[ "$RUN_ALL_MM" == "true" ]]; then
            pytest -vv --tb=short --self-contained-html --html=/tmp/test_multimodal_report/report.html \
              --junitxml=/tmp/test_multimodal_report/junit.xml "${MARKER_ARGS[@]}" "${IGNORE_ARGS[@]}"
          else
            pytest -vv --tb=short --self-contained-html --html=/tmp/test_multimodal_report/report.html \
              --junitxml=/tmp/test_multimodal_report/junit.xml "${MARKER_ARGS[@]}" "${MM_TEST_FILES[@]}"
          fi
          PYTEST_RC=$?
          set -e
          echo "MULTIMODAL_EXIT_CODE=$PYTEST_RC" >> "$GITHUB_ENV"

          # Print test results summary with failure details
          python3 - <<'PYEOF'
          import xml.etree.ElementTree as ET, os
          from collections import defaultdict
          def fmt_time(s):
              s = float(s); m = int(s) // 60; r = s - m * 60
              return f"{m}m{r:.0f}s" if m else f"{s:.1f}s"
          xml_path = '/tmp/test_multimodal_report/junit.xml'
          if not os.path.exists(xml_path):
              print("No test results found (JUnit XML not generated)")
          else:
              root = ET.parse(xml_path).getroot()
              files = defaultdict(lambda: {'total': 0, 'passed': 0, 'failed': 0, 'skipped': 0, 'errors': 0, 'time': 0.0})
              failures = []
              errors = []
              for tc in root.findall('.//testcase'):
                  parts = tc.get('classname', '').split('.')
                  fname = next((p for p in parts if p.startswith('test_')), None)
                  if not fname:
                      raw_name = tc.get('name', parts[0] if parts else 'unknown')
                      fname = raw_name.replace('.py', '') if raw_name else 'unknown'
                  test_id = f"{fname}::{tc.get('name', 'unknown')}"
                  files[fname]['total'] += 1
                  files[fname]['time'] += float(tc.get('time', 0))
                  fail_el = tc.find('failure')
                  err_el = tc.find('error')
                  if fail_el is not None:
                      files[fname]['failed'] += 1
                      msg = fail_el.get('message', '')[:200]
                      failures.append(f"  FAILED  {test_id}\n          {msg}")
                  elif err_el is not None:
                      files[fname]['errors'] += 1
                      msg = err_el.get('message', '')[:200]
                      errors.append(f"  ERROR   {test_id}\n          {msg}")
                  elif tc.find('skipped') is not None:
                      files[fname]['skipped'] += 1
                  else:
                      files[fname]['passed'] += 1
              print(f"\n{'Test File':<55} {'Run':>5} {'Pass':>5} {'Fail':>5} {'Skip':>5} {'Err':>5} {'Time':>8}")
              print('-' * 90)
              for fname, c in sorted(files.items()):
                  print(f"{fname:<55} {c['total']:>5} {c['passed']:>5} {c['failed']:>5} {c['skipped']:>5} {c['errors']:>5} {fmt_time(c['time']):>8}")
              if failures:
                  print(f"\n--- Failures ({len(failures)}) ---")
                  print('\n'.join(failures))
              if errors:
                  print(f"\n--- Errors ({len(errors)}) ---")
                  print('\n'.join(errors))
              print()
          PYEOF
          deactivate
          exit $PYTEST_RC

      - name: Upload Multimodal Weld Defect Detection Sample Apps HTML test report to GitHub
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882 # v4.4.3
        with:
          name: multimodal-test-report
          path: /tmp/test_multimodal_report

      - name: Generate Test Summary
        if: ${{ !cancelled() }}
        env:
          TAG: ${{ inputs.tag || github.ref }}
          BUILD: ${{ inputs.build || 'yes' }}
          TESTS: ${{ inputs.tests || env.DEFAULT_TESTS }}
          SKIP_LONG_TESTS: ${{ inputs.skip_long_tests || 'yes' }}
        run: |
          python3 - <<'PYEOF'
          import xml.etree.ElementTree as ET, os

          summary_path = os.environ['GITHUB_STEP_SUMMARY']
          tag     = os.environ.get('TAG', 'main')
          build   = os.environ.get('BUILD', 'yes')
          tests   = os.environ.get('TESTS', 'all-tests')
          skip_long = os.environ.get('SKIP_LONG_TESTS', 'yes')
          event   = os.environ.get('GITHUB_EVENT_NAME', 'unknown')
          ts_core_exit = os.environ.get('TIMESERIES_CORE_EXIT_CODE', '')
          ts_security_exit = os.environ.get('TIMESERIES_SECURITY_EXIT_CODE', '')
          mm_exit = os.environ.get('MULTIMODAL_EXIT_CODE', '')

          def fmt_time(s):
              s = float(s); m = int(s) // 60; r = s - m * 60
              return f"{m}m{r:.0f}s" if m else f"{s:.1f}s"

          def parse_junit(xml_path):
              if not os.path.exists(xml_path):
                  return None, []
              from collections import defaultdict
              root = ET.parse(xml_path).getroot()
              files = defaultdict(lambda: {'total': 0, 'passed': 0, 'failed': 0, 'skipped': 0, 'errors': 0, 'time': 0.0})
              issue_details = []
              for tc in root.findall('.//testcase'):
                  parts = tc.get('classname', '').split('.')
                  fname = next((p for p in parts if p.startswith('test_')), None)
                  # Fall back to the testcase 'name' attribute for collection errors
                  # where classname may be empty
                  if not fname:
                      raw_name = tc.get('name', parts[0] if parts else 'unknown')
                      fname = raw_name.replace('.py', '') if raw_name else 'unknown'
                  test_id = f"{fname}::{tc.get('name', 'unknown')}"
                  files[fname]['total'] += 1
                  files[fname]['time'] += float(tc.get('time', 0))
                  fail_el = tc.find('failure')
                  err_el = tc.find('error')
                  if fail_el is not None:
                      files[fname]['failed'] += 1
                      msg = fail_el.get('message', '')[:200]
                      issue_details.append(('FAIL', test_id, msg))
                  elif err_el is not None:
                      files[fname]['errors'] += 1
                      msg = err_el.get('message', '')[:200]
                      issue_details.append(('ERROR', test_id, msg))
                  elif tc.find('skipped') is not None:
                      files[fname]['skipped'] += 1
                  else:
                      files[fname]['passed'] += 1

              # Also capture testsuite-level errors (e.g. collection errors)
              # that prevented tests from running
              for ts in root.findall('.//testsuite'):
                  ts_errors = int(ts.get('errors', 0))
                  ts_tests = int(ts.get('tests', 0))
                  # If there are errors at the testsuite level but no testcases were run,
                  # report the collection/setup failure
                  if ts_errors > 0 and ts_tests == 0 and not files:
                      suite_name = ts.get('name', 'unknown')
                      files[suite_name] = {'total': ts_errors, 'passed': 0, 'failed': 0, 'skipped': 0, 'errors': ts_errors}

              return [{'name': n, **c} for n, c in sorted(files.items())], issue_details

          overall = {'total': 0, 'passed': 0, 'failed': 0, 'skipped': 0, 'errors': 0, 'time': 0.0}

          with open(summary_path, 'a') as out:
              out.write('## \U0001f4ca Test Execution Summary\n\n')

              out.write('### \U0001f50d Run Information\n')
              out.write(f'- **Event:** {event}\n')
              out.write(f'- **Tag/Branch:** {tag}\n')
              out.write(f'- **Build:** {build}\n')
              out.write(f'- **Tests:** {tests}\n')
              out.write(f'- **Skip Long Tests:** {skip_long}\n\n')

              sections = [
                  ('\U0001f30a Time Series Wind and Weld Tests', '/tmp/test_timeseries_core_report/junit.xml', ts_core_exit),
                  ('\U0001f512 Security Tests', '/tmp/test_timeseries_security_report/junit.xml', ts_security_exit),
                  ('\U0001f3ad Multimodal Tests', '/tmp/test_multimodal_report/junit.xml', mm_exit),
              ]
              for title, xml_path, exit_code in sections:
                  out.write(f'### {title}\n')
                  results, issues = parse_junit(xml_path)
                  if results is None:
                      out.write('\u23ed\ufe0f **Status:** SKIPPED (not run)\n\n')
                      continue
                  if exit_code == '0':
                      out.write('\u2705 **Status:** PASSED\n\n')
                  else:
                      out.write(f'\u274c **Status:** FAILED (Exit Code: {exit_code or "?"})\n\n')
                  out.write('| Test File | Total | Pass | Fail | Skip | Err | Time |\n')
                  out.write('|-----------|------:|-----:|-----:|-----:|----:|-----:|\n')
                  sec = {'total': 0, 'passed': 0, 'failed': 0, 'skipped': 0, 'errors': 0, 'time': 0.0}
                  for r in results:
                      out.write(f"| `{r['name']}` | {r['total']} | {r['passed']} | {r['failed']} | {r['skipped']} | {r['errors']} | {fmt_time(r['time'])} |\n")
                      for k in sec:
                          sec[k] += r[k]
                  out.write(f"| **Total** | **{sec['total']}** | **{sec['passed']}** | **{sec['failed']}** | **{sec['skipped']}** | **{sec['errors']}** | **{fmt_time(sec['time'])}** |\n\n")
                  if issues:
                      out.write('<details><summary>\u274c Failure/Error Details</summary>\n\n')
                      out.write('| Status | Test | Message |\n')
                      out.write('|--------|------|--------|\n')
                      for kind, test_id, msg in issues:
                          safe_msg = msg.replace('|', '\\|').replace('\n', ' ')
                          out.write(f'| {kind} | `{test_id}` | {safe_msg} |\n')
                      out.write('\n</details>\n\n')
                  for k in overall:
                      overall[k] += sec[k]

              out.write('### \U0001f3af Overall Summary\n')
              out.write('| Metric | Count |\n')
              out.write('|--------|------:|\n')
              out.write(f"| **Total Tests** | **{overall['total']}** |\n")
              out.write(f"| \u2705 Passed | {overall['passed']} |\n")
              out.write(f"| \u274c Failed | {overall['failed']} |\n")
              out.write(f"| \u23ed\ufe0f Skipped | {overall['skipped']} |\n")
              out.write(f"| \u26a0\ufe0f Errors | {overall['errors']} |\n")
              out.write(f"| \u23f1\ufe0f Total Duration | {fmt_time(overall['time'])} |\n\n")

              if overall['total'] > 0:
                  rate = (overall['passed'] * 100) // overall['total']
                  out.write(f'\U0001f4c8 **Success Rate:** {rate}%\n\n')
              else:
                  out.write('\U0001f4c8 **Success Rate:** N/A (No tests executed)\n\n')

              if overall['total'] == 0:
                  out.write('\U0001f50d **Overall Status:** NO TESTS EXECUTED\n')
              elif overall['failed'] == 0 and overall['errors'] == 0:
                  out.write('\u2705 **Overall Status:** ALL TESTS PASSED\n')
              else:
                  out.write('\u274c **Overall Status:** SOME TESTS FAILED\n')
          PYEOF
