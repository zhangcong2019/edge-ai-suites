---
# SPDX-FileCopyrightText: (C) 2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

name: Skill Scanner

on:
  pull_request:
    paths:
      - ".github/skills/**"
      - "**/.github/skills/**"
      - ".github/workflows/skill-scan.yaml"
  push:
    branches: ["main"]
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      severity:
        description: "SkillSpector severity level (CRITICAL, HIGH, MEDIUM, LOW)"
        required: false
        default: "LOW"
        type: choice
        options:
          - CRITICAL
          - HIGH
          - MEDIUM
          - LOW

permissions: {}

jobs:
  discover-skills:
    name: Discover skill paths
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      matrix: ${{ steps.find-paths.outputs.matrix }}
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Find all .github/skills directories
        id: find-paths
        run: |
          paths=$(find . -path '*/.github/skills' -type d \
            | sed 's|^\./||' \
            | jq -R -s -c 'split("\n") | map(select(length > 0))')
          echo "matrix=$paths" >> "$GITHUB_OUTPUT"

  skillspector-scan:
    name: SkillSpector security scan (${{ matrix.skills-path }})
    needs: discover-skills
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        skills-path: ${{ fromJson(needs.discover-skills.outputs.matrix) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Run SkillSpector scan
        uses: open-edge-platform/geti-ci/actions/skill-scan@78e175e3e91f997e168a09aadd1933da95c1ac98 # skill-scan/v0.1.0
        with:
          skills-path: ${{ matrix.skills-path }}
          severity-level: ${{ github.event_name == 'pull_request' && 'HIGH' || github.event.inputs.severity || 'LOW' }}
          fail-on-findings: ${{ github.event_name == 'pull_request' && 'true' || 'false' }}

  skill-validator-scan:
    name: skill-validator scan (${{ matrix.skills-path }})
    needs: discover-skills
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        skills-path: ${{ fromJson(needs.discover-skills.outputs.matrix) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          fetch-depth: 0

      # v0.2.0 adds support for non-standard directories and root-level files inside a skill.
      # The default `allow-dirs` is "evals,example-prompts", which together with the standard
      # scripts/references/assets layout covers most skills in this repository. The metro
      # skills (metro-ai-apps-builder, metro-ai-apps-recipe) also ship a `benchmark/` directory,
      # so it is added below. Extend this list (e.g. allow-dirs: "evals,example-prompts,benchmark,my_custom_folder")
      # only when a skill introduces another directory.
      - name: Run skill-validator
        uses: open-edge-platform/geti-ci/actions/skill-validator@c64543e05e9f84c7c4ef212666c3ea5fc869e55a # skill-validator/v0.2.0
        with:
          skills-path: ${{ matrix.skills-path }}
          allow-dirs: "evals,example-prompts,benchmark"
          strict: ${{ github.event_name == 'pull_request' && 'true' || 'false' }}
          fail-on-findings: ${{ github.event_name == 'pull_request' && 'true' || 'false' }}
