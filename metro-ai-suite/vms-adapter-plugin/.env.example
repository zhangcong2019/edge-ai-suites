# PostgreSQL
PG_USER=vms
PG_PASSWORD=changeme
PG_HOST=postgres
PG_PORT=5434

# Nx Witness
NX_HOST=<nx-witness-ip>
NX_USERNAME=admin
NX_PASSWORD=changeme
NX_TLS_VERIFY=false
# Optional: path inside vms-adapter-backend container when NX_TLS_VERIFY=true
NX_CA_BUNDLE=
NX_RECORDINGS_PATH=/tmp/nx-recordings

# Live Video Captioning Analytics App
# FastAPI backend of the LVC stack (default port 4173)
LVC_HOST=host.docker.internal
LVC_BASE_URL=http://host.docker.internal:4173
# MediaMTX WebRTC signaling server (default port 8889)
MEDIAMTX_URL=http://host.docker.internal:8889

# DLS Vision App
DLS_VISION_HOST=host.docker.internal
DLS_VISION_PORT=443
DLS_VISION_TLS_VERIFY=false
# device specific DLS pipelines. At least one required
DLS_PIPELINE_CPU=
DLS_PIPELINE_GPU=
DLS_PIPELINE_NPU=
# Optional: path inside vms-adapter-backend container when DLS_VISION_TLS_VERIFY=true
DLS_VISION_CA_BUNDLE=

# Ports
UI_HTTPS_PORT=3443

# TLS Certificates for UI (optional)
# By default, a self-signed certificate is auto-generated on startup.
# To use your own certificates, copy docker-compose.tls.yml to
# docker-compose.override.yml and place cert.pem + key.pem in ./certs/ui/.

# VMS Plugin API
VMS_PLUGIN_API_KEY=

# MQTT Broker (eclipse-mosquitto in the LVC stack, port 1883)
MQTT_BROKER_HOST=host.docker.internal
MQTT_BROKER_PORT=1883
# TLS for the LVC MQTT broker connection (VAP as subscriber)
# NOTE: The MQTT broker must be independently configured with TLS.
MQTT_BROKER_TLS_ENABLED=false
# Path inside the vms-adapter-backend container to the CA cert for broker cert verification.
MQTT_BROKER_CA_BUNDLE=
# Paths to client cert/key for mutual TLS (leave empty for server-only TLS).
MQTT_BROKER_CLIENT_CERT=
MQTT_BROKER_CLIENT_KEY=

# DLS Vision App MQTT — broker address as seen by VAP (for subscribing)
MQTT_HOST=host.docker.internal
MQTT_PORT=1883
# TLS for the DLS Vision MQTT subscriber (same broker-side prerequisites apply)
MQTT_TLS_ENABLED=false
MQTT_CA_BUNDLE=
MQTT_CLIENT_CERT=
MQTT_CLIENT_KEY=
