apiVersion: 1

datasources:
  - name: Infinity
    type: yesoreyeram-infinity-datasource
    access: proxy
    isDefault: true
    jsonData:
    
  - name: grafana-mqtt-datasource
    type: grafana-mqtt-datasource
    access: proxy
    readOnly: false
    editable: true
    jsonData: { "uri": "tcp://0.0.0.0:1883" }