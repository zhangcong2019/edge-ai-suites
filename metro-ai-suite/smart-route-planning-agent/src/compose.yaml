# Copyright (C) 2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

networks:
  routeplanner:
    driver: bridge

services:
  route-planner-agent:
    image: intel/smart-route-planning-agent:2026.2.0-rc1
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USER_ID: ${USER_ID:-1000}
        USER_GROUP_ID: ${USER_GROUP_ID:-1000}
    ports:
      - "${AI_ROUTE_PLANNER_PORT:-7864}:7860"
    environment:
      - http_proxy=${http_proxy}
      - https_proxy=${https_proxy}
      - no_proxy=${no_proxy},${HOST_IP}
      - NO_PROXY=${no_proxy},${HOST_IP}
      - HTTP_PROXY=${http_proxy}
      - HTTPS_PROXY=${https_proxy}
    volumes:
      - ./data/config.json:/app/data/config.json:ro
    networks:
      - routeplanner
    healthcheck:
      test: ["CMD", "uv", "run", "health_check.py", "7860"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
