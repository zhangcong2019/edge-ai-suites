{{/*
Copyright (C) 2026 Intel Corporation
SPDX-License-Identifier: Apache-2.0
*/}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "vdms-vectordb.fullname" . }}
  labels:
    {{ include "vdms-vectordb.labels" . | nindent 4 }}
    app.kubernetes.io/component: vdms-vectordb
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "vdms-vectordb.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPortName }}
      protocol: TCP
      name: {{ .Values.service.portName }}
