ros-jazzy-benchmark-framework (0.2.6) UNRELEASED; urgency=low

  * system_wide_resource_attribution: psutil-based system-wide resource
    capture with post-hoc ROS2 process-tree attribution; drop RAPL/thermal
    monitoring; shared post-run analysis menu.
    - New _psutil_probe.py sampler replaces pidstat everywhere (incl.
      --continuous); writes a single JSON document (resource_usage.json/
      _ros2.json) with ROS2 process-tree attribution and PID -> node name
      mapping. visualize_resources.py, view_average.py, prometheus_exporter.py,
      aggregate_kpi.py updated for the new format.
    - Removed RAPL CPU power/thermal throttling support.
    - New shared src/_session_menu.sh post-run analysis module, used by both
      benchmark_runner.sh and bench_menu.sh.
    - Added min/max latency fields to Level 1 KPI output.
    - monitor_resources.py --io now collects real per-process disk I/O
      (delta read/write bytes per tick), with an optional plot in
      visualize_resources.py.
    - New monitor_resources.py -x/--ctx-switches (also in monitor_stack.py)
      collects per-process voluntary/involuntary context-switch deltas, a
      privilege-free signal of CPU contention, with an optional plot in
      visualize_resources.py.
    - Fixed ros2_graph_monitor.py bugs causing analyze_trigger_latency.py to
      report 0 input->output pairs: mixed wall-clock/sim-time timestamps on
      the same topic, and message callbacks starved by sharing a thread with
      periodic graph discovery/display.

 -- ECI Maintainer <eci.maintainer@intel.com>  Fri, 24 Jul 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.2.5) UNRELEASED; urgency=low

  * Added ADBScan benchmark

 -- ECI Maintainer <eci.maintainer@intel.com>  Fri, 24 Jul 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.2.4) UNRELEASED; urgency=low

  * post_run_analysis_prompt: centralized logging, CI-friendly benchmark
    targets, interactive post-run/post-benchmark analysis menus.
    - src/log_config.py: new shared logging module; get_logger()/
      configure_logging() resolve verbosity from --log-level CLI arg >
      LOG_LEVEL env var > .env file at repo root (simple parser, no
      python-dotenv dependency) > default INFO; logs to stdout (not
      logging's stderr default) so 2>/dev/null shell wrappers used to
      silence third-party stderr noise don't silence our own output too.
    - src/*.py: convert print() to logger.info/warning/error() across all
      scripts (854 call sites). INFO = progress/status messages; DEBUG =
      full per-run and aggregate result tables; WARNING = quiet/CI mode,
      with "no data found"-style messages kept visible at WARNING/ERROR
      so real problems aren't silently swallowed.
    - src/bench_menu.sh: new interactive post-benchmark menu (summary,
      E2E latency, per-run drill-down); run-picker accepts 0 or Enter to
      cancel instead of erroring.
    - src/benchmark_runner.sh: add --no-prompt for non-interactive/CI use;
      add interactive post-run analysis menu; disable EXIT trap after
      manual shutdown to avoid duplicate cleanup; validate numeric menu
      input under set -e.
    - Makefile: *-benchmark targets accept NO_PROMPT=1 to force --no-prompt
      on every run (including the last), skip the interactive post-
      benchmark menu, and force --log-level WARNING for aggregate_kpi.py;
      benchmark banners display the effective log level.
    - .github/linters/.pylintrc: disable logging-fstring-interpolation/
      logging-format-interpolation/logging-not-lazy to allow f-strings/
      .format() in log calls repo-wide.

 -- ECI Maintainer <eci.maintainer@intel.com>  Thu, 09 Jul 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.2.3) UNRELEASED; urgency=low

  * use_intel_python_libs: Intel-optimised Python acceleration layer.
    - pyproject.toml: add optional [dependency-groups] intel containing mkl,
      mkl-service, numexpr, dpnp, and pillow-simd (AVX2-accelerated drop-in for
      pillow, valid for all Intel iGPU targets); pillow removed from direct
      dependencies as it is already provided transitively by matplotlib.
    - src/_accel.py: new acceleration shim; imports dpnp as np (Intel oneAPI
      SYCL / level-zero, dispatches array ops to iGPU) with transparent
      fallback to numpy when dpnp is absent; exposes numexpr as ne; calls
      mkl.set_num_threads(nproc) on import when MKL is available.
    - src/visualize_gpu.py, visualize_graph.py, visualize_kpi.py,
      visualize_npu.py, visualize_resources.py, visualize_timing.py,
      view_average.py: replace `import numpy as np` with
      `from _accel import np` so all array work goes through the shim.
    - src/visualize_timing.py, view_average.py: replace element-wise
      normalisation expressions with numexpr.evaluate() calls for
      VML-accelerated multi-threaded evaluation on Intel CPUs.

 -- ECI Maintainer <eci.maintainer@intel.com>  Tue, 24 Jun 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.2.2) UNRELEASED; urgency=low

  * Install Python scripts as executable (755); document cp-first workflow
    for Debian package users in INSTALL.md and Makefile help.

 -- ECI Maintainer <eci.maintainer@intel.com>  Mon, 09 Jun 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.2.1) UNRELEASED; urgency=low

  * final_fixes: power monitoring, thermal charts, default record-plot, report.
    - src/monitor_stack.py: add --power CLI flag for RAPL CPU package power
      monitoring; wired to enable_power in MonitoringSession.
    - src/benchmark_runner.sh: pass --power to monitor_stack so cpu_power.log
      is always written; add [N/6] stage progress labels throughout each run;
      add --show flag (implies --record --plot) that auto-invokes make results
      after the run.
    - src/visualize_thermal.py: add plot_temperature_only() and plot_power_only()
      producing standalone thermal_temperature.png and thermal_power.png
      alongside the existing combined thermal_throttle.png when --save is used.
    - src/generate_report.py: handle missing kpi.json gracefully (warn + write
      partial report) instead of crashing with FileNotFoundError.
    - Makefile: plain make wandering / fastmapping / picknplace-run now default
      to --record --plot; add SHOW= passthrough to all scenario targets;
      results target adds [0/2] kpi.json recovery step from bag when missing,
      fixes file:// URL for absolute SESSION paths, uses $(CURDIR) for
      find auto-detect, adds headless http.server hint; benchmark targets
      print aggregate progress note before aggregate_kpi.py call.
    - debian/rules: add config/ to cp -r install so YAML run profiles are
      shipped in the package.

 -- ECI Maintainer <eci.maintainer@intel.com>  Sun, 08 Jun 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.2.0) UNRELEASED; urgency=low

  * YAML-driven run profiles and generic benchmark orchestrator.
    - src/benchmark_profiler.py: new Python YAML loader/validator for run
      profiles; exports all scenario settings as CONF_* shell variables via
      --export-bash; also usable as a Python module.
    - src/benchmark_runner.sh: new generic scenario-agnostic orchestrator;
      replaces per-scenario monolithic scripts; driven entirely by YAML config.
    - config/wandering_run.yaml: run profile for wandering scenario.
    - config/picknplace_run.yaml: run profile for pick-and-place scenario.
    - config/fastmapping_run.yaml: run profile for fast-mapping scenario;
      uses ros2 launch fast_mapping fast_mapping.launch.py (docs approach).
    - src/wandering_run.sh: replaced 300-line script with 22-line exec wrapper.
    - src/picknplace_run.sh: replaced 270-line script with 22-line exec wrapper.
    - src/fastmapping_run.sh: replaced 300-line script with 25-line exec wrapper.
    - Makefile: add RUN_CONFIG passthrough to all wandering-*, picknplace-*,
      and fastmapping-* targets.

 -- ECI Maintainer <eci.maintainer@intel.com>  Fri, 29 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.19) UNRELEASED; urgency=low

  * Thermal visualizer, session browser, and benchmarking bug fixes.
    - src/visualize_thermal.py: 3-panel CPU/GPU temperature, throttle, and
      package-power dashboard; saves PNG; auto-detects latest session.
    - src/show_sessions.py: tree-view of all monitoring sessions with per-run
      duration, e2e latency, throughput, drop rate, goals, and throttle flag.
    - src/analyze_bag_e2e.py: fix Level 2 KPI reporting 0 ms latency when
      entry and exit topics resolved to the same topic.
    - src/wandering_run.sh: print 'make results' banner after each run.
    - Makefile: add results, visualize-thermal, show-sessions targets; fix
      clean-last for bench subdirectories; forward GOALS in wandering-benchmark.

 -- ECI Maintainer <eci.maintainer@intel.com>  Wed, 21 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.18) UNRELEASED; urgency=low

  * KPI visualization CLI.
    - src/visualize_kpi.py: new CLI chart generator producing publication-ready
      PNG/SVG charts from kpi.json benchmark output.
      latency_histogram: horizontal grouped bar chart of per-node mean/p50/p90
        latency, colour-coded by pipeline stage (Sensor/Perception/Planning/
        Control), with inline value labels and dual legends.
      sku_comparison: grouped bar chart comparing mean latency per node across
        multiple SKUs; p90 shown as a tick marker overlay; accepts >= 2 --kpi
        files with optional --label overrides.
      resource_utilization: horizontal bar chart of CPU utilization and thermal
        readings (cpu_mean_pct, cpu_max_pct, cpu/gpu/npu temp_c); skips
        gracefully when all fields are null (WSL2/no-sysfs environments);
        annotates throttled components with a warning marker.
      _infer_sku_label: auto-detects Intel SKU family (PTL/LNL/ARL/MTL/
        RPL-R/RPL/ADL-N/ADL/TGL/Xeon/Atom) from metadata.hardware.cpu_model
        so --label is optional for cross-SKU comparisons.
    - tests/test_visualize_kpi.py: 41 unit tests; all pass headlessly via
      the Agg matplotlib backend.
    - Makefile: visualize-kpi target with SESSION= and FORMAT= parameters.

 -- ECI Maintainer <eci.maintainer@intel.com>  Sun, 18 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.17) UNRELEASED; urgency=low

  * Add Intel RAPL CPU package power monitoring (ITEP-90266).
    - monitor_resources.py: probe_cpu_power_available(), monitor_cpu_power()
      daemon reads /sys/class/powercap/intel-rapl/intel-rapl:0/energy_uj at
      configurable interval; handles counter wraparound; writes cpu_power.log
      (JSON-lines). New --power / --power-log CLI flags; --check-hw now
      reports RAPL availability alongside GPU/NPU.
    - monitor_stack.py: enable_power flag; auto-detects RAPL via
      probe_cpu_power_available() and passes --power --power-log to the
      resource monitor subprocess when available.
    - analyze_trigger_latency.py: reads cpu_power.log, averages power_w
      samples, stores mean as cpu_pkg_power_w in the thermal KPI section.
    - schemas/kpi_level1_v1.json: add cpu_pkg_power_w (number|null) to
      thermal object (additionalProperties: false).
    - prometheus_exporter.py: ros2_cpu_package_power_watts Gauge reads
      the latest sample from cpu_power.log on each scrape.

 -- ECI Maintainer <eci.maintainer@intel.com>  Thu, 15 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.16) UNRELEASED; urgency=low

  * Bag-replay benchmarking without a live robot or simulator.
    - src/bag_replay_run.sh: new script — replays any ROS 2 bag at configurable
      rate/loop count through the monitor stack; collects Level 1 and Level 2
      KPIs deterministically; supports --rate, --loop, --plot, --output-parent.
    - Makefile: bag-replay, bag-replay-plot, bag-replay-benchmark targets.
  * fast_mapping benchmark pipeline.
    - src/fastmapping_run.sh: launches fast_mapping_node + bag replay of the
      bundled spinning RGB-D bag (/opt/ros/<distro>/share/bagfiles/spinning);
      auto-detects ROS distro; stops node gracefully and waits for procedure
      table flush before analysis.
    - src/analyze_fastmapping_log.py: parses fast_mapping_node.log to extract
      per-procedure timing (wait/preprocess/octree/publish) and patches kpi.json
      in-place with throughput_hz, mean_latency_ms, jitter fields, per_node and
      pairs entries; falls back to window-based KPIs when node is killed before
      shutdown table is printed.
    - Makefile: fastmapping, fastmapping-plot, fastmapping-benchmark targets;
      FASTMAPPING_BAG points to the bundled spinning bag.
  * GPU visualization fixes.
    - src/gpu_engine_defs.py: ENGINE_CLASSES regex now matches i915 fdinfo key
      names ("copy" → Blitter, "video-enhance" → VE).
    - src/visualize_gpu.py: dynamic y-axis (no 100 % ceiling clip); new
      per-engine detail panel; MaxNLocator prevents MAXTICKS warnings at >100 %
      busy; frequency panel shows graceful fallback message when all-zero.
    - src/monitor_resources.py: supplement i915 GT frequency from sysfs
      gt_act_freq_mhz / gt_max_freq_mhz when qmassa reports 0.

 -- ECI Maintainer <eci.maintainer@intel.com>  Tue, 13 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.15) UNRELEASED; urgency=low

  * Add NPU throttle detection to sysfs monitor.
    - monitor_resources.py: _read_sysfs_npu() now emits 'throttled' bool
      (True when cur_freq_mhz < max_freq_mhz * 0.95); console output shows
      ⚠THROTTLE indicator when active.
    - analyze_trigger_latency.py: _load_resource_thermal() reads 'throttled'
      from npu_usage.log; 'npu_throttled' in thermal KPI section is now
      True/False (not always None) when npu_usage.log is present.
    - Dependency security: bump pillow >=12.2.0 (5 CVEs), pytest >=9.0.3
      (1 CVE), add pygments >=2.20.0 (1 CVE) to dev deps.

 -- ECI Maintainer <eci.maintainer@intel.com>  Fri, 08 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.14) UNRELEASED; urgency=low

  * Correlate GPU/NPU thermal and throttle data with Level 1 JSON output
   .
    - analyze_trigger_latency.py: add _read_cpu_thermal_sysfs() and
      _load_resource_thermal() helpers; build_performance_kpi() now includes
      a 'thermal' section: cpu_temp_c, gpu_temp_c, npu_temp_c,
      cpu_throttled, gpu_throttled, npu_throttled.
    - monitor_resources.py: add _read_cpu_thermal_sysfs() helper; reads
      x86_pkg_temp sysfs zone and cpufreq scaling_cur_freq for throttle
      detection.
    - schemas/kpi_level1_v1.json: add optional 'thermal' object property
      with all six fields; existing KPI files without 'thermal' remain valid.

 -- ECI Maintainer <eci.maintainer@intel.com>  Fri, 08 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.13) UNRELEASED; urgency=low

  * Remove intel_gpu_top entirely; use qmassa exclusively for GPU monitoring.
    - monitor_resources.py: remove _try_intel_gpu_top_local, _find_local_igt,
      _parse_igt_clients; simplify probe_gpu_available and monitor_gpu to
      qmassa-only (sysfs RC6 fallback retained for remote sessions).
    - gpu_pid_analyzer.py: full rewrite for qmassa; remove --remote-ip/--remote-user;
      update print_snapshot, CSV output, collect_snapshot and main().
    - visualize_gpu.py: remove intel_gpu_top source guards; engine/power panels
      now work for any qmassa record.
    - visualize_resources.py: remove intel_gpu_top source guards from has_power,
      has_engines, and frequency panel (RC6 overlay removed).
    - monitor_stack.py: update --gpu help text (no CAP_PERFMON required).
    - Makefile: remove setup-remote-gpu target.

 -- ECI Maintainer <eci.maintainer@intel.com>  Tue, 06 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.12) UNRELEASED; urgency=low

  * Add 'make install-qmassa' target: installs Rust toolchain via rustup
    (minimal profile) if cargo is absent, then runs
    cargo install --locked qmassa qmmd; binaries land in ~/.cargo/bin/.

 -- ECI Maintainer <eci.maintainer@intel.com>  Tue, 06 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.11) UNRELEASED; urgency=low

  * Add qmassa/xe GPU monitoring support in src/monitor_resources.py:
    auto-detects active DRM driver (xe vs i915) at runtime via sysfs symlink;
    xe systems use qmassa -x (headless JSON) for per-PID engine utilisation,
    power, throttle, VRAM/SMEM, and temperature (dGPU); i915 systems continue
    to use intel_gpu_top -J; both fall back to sysfs RC6 residency on failure.
  * Add _try_qmassa_local(), _find_local_qmassa(), _detect_gpu_driver() helpers
    in src/monitor_resources.py; search ~/.cargo/bin, /usr/local/bin, etc.
  * Update src/gpu_engine_defs.py: add Compute engine class (xe ccs); extend
    Render/3D, Blitter, Video, VE regexes to match xe names (rcs, bcs, vcs,
    vecs) alongside existing i915 names.
  * Add _PmtTelemetry class to src/monitor_resources.py: reads Intel PMT binary
    sysfs interface (/sys/class/intel_pmt/telem*) to provide NPU power (W),
    junction temperature (degC), and NoC memory bandwidth (MB/s); auto-detects
    SKU via GUID (MTL, ARL, ARL-H/S, LNL, PTL, WCL); purely additive — SSH
    remote monitoring path unaffected.
  * Extend _read_sysfs_npu() to bracket its sysfs delta-sample with PMT
    pre/post reads; adds power_w, temp_c, bw_mbps, pmt_gen fields to
    npu_usage.log JSON-lines when PMT is available.
  * Extend monitor_npu() console output to show power, temperature, and BW
    when present.

 -- ECI Maintainer <eci.maintainer@intel.com>  Mon, 05 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.10) UNRELEASED; urgency=low

  * Add regression-check CI job to .github/workflows/build-test-scan.yml:
    runs Level 1 and Level 2 baseline-vs-baseline smoke tests after unit-tests
    job, gates on exit code from compare_kpi.py.
  * Add regression-check-l2 Makefile target: symmetric with regression-check
    for Level 2 KPI JSON comparisons.
  * Update regression-check Makefile target help comment to clarify Level 1 scope.

 -- ECI Maintainer <eci.maintainer@intel.com>  Mon, 05 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.9) UNRELEASED; urgency=low

  * Migrate test suite to pytest framework; all tests runnable via 'make test'
    or 'uv run pytest tests/ -v' without ROS 2, robot, or hardware.
  * Add tests/conftest.py: centralises sys.path setup for all test modules.
  * Add tests/fixtures.py: single source of truth for synthetic Level 1 and
    Level 2 KPI payloads shared across test files.
  * Add tests/test_aggregate_kpi.py (37 tests): boundary-condition coverage for
    _health, _consistency, _classify; aggregate() statistics, sort order,
    min-runs filtering, node_filter, and zero-stdev single-run edge case.
  * Add tests/test_trigger_latency.py (30 tests): _is_internal topic-filter
    regex (13 filtered + 8 pass-through topics); find_trigger binary-search
    (9 edge cases including empty list, exact match, before-all timestamps).
  * Add tests/test_wandering_metrics.py (25 tests): _extract_goals,
    _extract_elapsed, _extract_rtf, _extract_hz, _verdict regex extractors
    with spacing variants, absent fields, and multi-block cases.
  * Fix schemas/kpi_level1_v1.json: add missing 'pipeline_stage' field to
    'pairs' items (additionalProperties: false was rejecting valid output).
  * Add 'make test' target to Makefile.
  * Update CI (.github/workflows/build-test-scan.yml): replace three separate
    'python3 tests/test_*.py' steps with single 'uv run pytest tests/ -v'.
  * Update docs/COMMANDS.md: Unit Tests section; make test in Quick Reference.
  * Update docs/IMPROVEMENTS.md: May 2026 entry documenting test suite.

 -- ECI Maintainer <eci.maintainer@intel.com>  Mon, 04 May 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.8) UNRELEASED; urgency=low

  * Add src/compare_kpi.py: KPI regression detection CLI (--baseline, --current,
    --threshold, --report). Supports Level 1 and Level 2 JSON outputs.
  * Add synthetic baseline fixtures and unit tests for regression detection.

 -- ECI Maintainer <eci.maintainer@intel.com>  Tue, 29 Apr 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.7) UNRELEASED; urgency=low

  * Add --csv-out flag to analyze_trigger_latency.py: writes a flat CSV with
    one row per node/input→output pair. Columns: session, node, pipeline_stage,
    input, output, n, mean_ms, stdev_ms, min_ms, p50_ms, p90_ms, p99_ms,
    max_ms, trigger_count, fps, jitter_mean_ms, jitter_max_ms.
  * Add --csv-out flag to analyze_pipeline_latency.py: writes a flat CSV with
    one e2e summary row and one row per pipeline stage. Columns: type, session,
    stage, representative_node, representative_input, representative_output,
    mean_ms, p50_ms, p90_ms, p99_ms, max_ms, n, throughput_hz, drop_rate_pct,
    bottleneck_stage, cpu_mean_pct, cpu_max_pct.
  * Add --xlsx-out flag to both scripts: writes the same columns as --csv-out
    to an Excel workbook with bold headers and auto-width columns.
    Requires openpyxl (pip install openpyxl); gracefully skips if not installed.
  * Add CSV/Excel export usage examples to CHEATSHEET.txt.
  * Add tests/test_csv_export.py: smoke tests for --csv-out flag presence
    and end-to-end Level 2 CSV generation using a synthetic kpi.json fixture;
    no ROS install required.
  * Add unit-tests job to .github/workflows/build-test-scan.yml running
    tests/test_csv_export.py on every push/PR to main.

 -- ECI Maintainer <eci.maintainer@intel.com>  Mon, 28 Apr 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.6) UNRELEASED; urgency=low

  * Add Level 2 end-to-end pipeline KPI schema and analysis
    script to satisfy (pipeline-level KPIs).
  * Add schemas/kpi_level2_v1.json (JSON Schema Draft 2020-12) covering:
    - pipeline entry/exit topics and stage sequence
    - e2e_latency_ms: mean/p50/p90/p99/max/n (method='chained', summed from
      per-stage Level 1 representative pairs)
    - throughput_hz: bottleneck stage rate (min across stages)
    - drop_rate_pct: estimated message drop from sensor to control stage
    - bottleneck_stage: stage with highest mean latency contribution
    - stage_latency_ms: per-stage breakdown (Sensor/Perception/Planning/Control)
      with representative node, input/output topics, and sample count
    - cpu_mean_pct / cpu_max_pct (null when not collected)
    - level1_source: provenance path to source kpi.json
    - metadata: same structure as Level 1 (includes hardware fields)
  * Add src/analyze_pipeline_latency.py: reads Level 1 kpi.json and writes
    kpi_level2.json, validated against schemas/kpi_level2_v1.json via
    jsonschema Draft202012Validator; auto-discovers most-recent session
    when --kpi is not specified.
  * Add quickstart option 4→6 'Level 2 pipeline KPI' to run the above
    analysis on the most recent wandering session's kpi.json.

 -- ECI Maintainer <eci.maintainer@intel.com>  Thu, 24 Apr 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.5) UNRELEASED; urgency=low

  * Fix timestamp accuracy in ros2_graph_monitor.py: replace time.time()
    (monitor wall-clock, adds DDS transport + Python GIL jitter) with
    msg.header.stamp (publisher's own clock at publish time) for all
    messages that carry a std_msgs/Header. For headerless messages
    (e.g. geometry_msgs/Twist cmd_vel) fall back to self.get_clock().now()
    which respects use_sim_time and keeps all timestamps on the same
    time base. Add monotonicity guard to reject negative inter-message
    intervals caused by non-monotonic sim-clock jumps.
  * Add --use-sim-time flag to ros2_graph_monitor.py and monitor_stack.py.
    Auto-detect simulation mode at startup from /clock topic presence;
    flag only needed to force early enablement before auto-detection fires.
  * Pass --use-sim-time explicitly from wandering_run.sh and
    picknplace_run.sh (Gazebo sim always runs with /clock).
  * Add framework_version, ros_distro, and hardware (cpu_model, cpu_cores,
    gpu_model, total_ram_gb) to kpi.json metadata block; schema updated to require these fields and validate their structure.
    framework_version read from pyproject.toml via importlib.metadata;
    ros_distro from ROS_DISTRO env; hardware collected via /proc/cpuinfo,
    os.cpu_count(), lspci, and psutil.

 -- ECI Maintainer <eci.maintainer@intel.com>  Thu, 24 Apr 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.4) UNRELEASED; urgency=low

  * Fix missing schemas/ directory in debian/rules install step; schemas/
    was not copied to the install destination, causing schema validation
    to fail with 'Schema file not found' at runtime
  * Fix quickstart 'Analyze last wandering/pick-n-place session bag' options:
    bag discovery pattern changed from 'rosbag2_*' to 'bag' to match the
    output name used by wandering_run.sh and picknplace_run.sh; switch from
    analyze_rosbag.py (file-based) to analyze_trigger_latency.py --bag
    (directory-based, produces KPI output)
  * Fix MCAP bag read warning 'attempted to read in receive timestamp order
    with no message index': explicitly set SequentialReader read order to
    RecordedTimeStamp, which requires no per-message index and is correct
    for latency analysis

 -- ECI Maintainer <eci.maintainer@intel.com>  Thu, 24 Apr 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.3) UNRELEASED; urgency=low

  * Add Level 1 KPI JSON schema (schemas/kpi_level1_v1.json, Draft 2020-12)
  * Add schema_version field to per-session KPI JSON output
  * Add validate_kpi_json() to analyze_trigger_latency.py; validates output
    against schema after every --json-out write
  * Add jsonschema>=4.0.0 to project dependencies
  * Fix grafana-status make target missing its target header
  * Fix shell command substitution in grafana-status ($(ls) -> $$(ls))
  * Replace broken src/Makefile delegation with direct implementations for
    all simulation/benchmark make targets (wandering, picknplace, analyze-benchmark)
  * Simplify process-sweep comments in wandering_run.sh and picknplace_run.sh

 -- ECI Maintainer <eci.maintainer@intel.com>  Mon, 21 Apr 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.2) UNRELEASED; urgency=low

  * Add structured per-session JSON output with throughput, latency, jitter,
    and metadata fields to analyze_trigger_latency.py
  * Add per-component Performance Summary table to terminal output
  * Add throughput (Hz) column and Aggregate Summary table to aggregate_kpi.py

 -- ECI Maintainer <eci.maintainer@intel.com>  Fri, 03 Apr 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.1) UNRELEASED; urgency=low

  * Add simulation benchmarking scripts and fix make targets

 -- ECI Maintainer <eci.maintainer@intel.com>  Fri, 03 Apr 2026 00:00:00 +0000

ros-jazzy-benchmark-framework (0.1.0) UNRELEASED; urgency=low

  * Initial release of ROS2 KPI Monitoring Stack

 -- ECI Maintainer <eci.maintainer@intel.com>  Thu, 27 Mar 2026 00:00:00 +0000
